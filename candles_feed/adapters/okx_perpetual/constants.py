"""
Constants for the OKX perpetual adapter.

This module imports constants from the base OKX module.
"""

from candles_feed.adapters.okx.constants import (
    INTERVAL_TO_OKX_FORMAT,
    INTERVALS,
    MAX_RESULTS_PER_CANDLESTICK_REST_REQUEST,
    PERP_CANDLES_ENDPOINT as CANDLES_ENDPOINT,
    PERP_REST_URL as REST_URL,
    PERP_WSS_URL as WSS_URL,
    WS_INTERVALS,
)